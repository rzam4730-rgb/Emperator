# Required in production: generate a long random value (at least 32 bytes).
EMPERATOR_JWT_SECRET=replace-with-a-long-random-secret

# Optional token lifetimes.
EMPERATOR_ACCESS_MINUTES=15
EMPERATOR_REFRESH_DAYS=30
